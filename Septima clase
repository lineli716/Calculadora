using System;
using System.Collections.Generic;
using System.Text;

namespace Calculadora
{
    class Division
    {
        public double division(double a, double b)
        {
            return (a / b);
        }
    }
}
